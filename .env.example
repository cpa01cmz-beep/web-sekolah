# Cloudflare Workers Environment Variables

# Environment
# Current environment: development, staging, or production
ENVIRONMENT=development

# Security: CORS Configuration
# Comma-separated list of allowed origins for API requests
# Example: https://example.com,https://www.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4173

# Security: JWT Secret Key
# Used to sign and verify JWT tokens
# ⚠️ CRITICAL: Generate a strong random string for production (minimum 64 characters recommended)
# Example: openssl rand -base64 64
# Do NOT use the placeholder value in production
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_MINIMUM_64_CHARACTERS

# Cloudflare Workers Configuration
# These are typically set by Wrangler automatically
# CF_ACCOUNT_ID=your-account-id
# CF_API_TOKEN=your-api-token

# Application Logging
# Log level for application logging (default: debug in dev, info in production)
# Options: debug, info, warn, error
VITE_LOG_LEVEL=info
