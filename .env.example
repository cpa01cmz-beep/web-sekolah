# Cloudflare Workers Environment Variables

# Environment
# Current environment: development, staging, or production
ENVIRONMENT=development

# Security: CORS Configuration
# Comma-separated list of allowed origins for API requests
# ⚠️ CRITICAL: Update to production domain(s) before deployment
# Example: https://example.com,https://www.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4173

# Security: JWT Secret Key
# Used to sign and verify JWT tokens
# ⚠️ CRITICAL: Generate a strong random string for production (minimum 64 characters recommended)
# Example: openssl rand -base64 64
# ⚠️ CRITICAL: Never commit actual secrets to version control
# ⚠️ CRITICAL: Use different secrets for development, staging, and production
# ⚠️ CRITICAL: Rotate secrets annually or if suspected compromise

# Production JWT Secret
# Used for production environment (your-domain.workers.dev)
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_MINIMUM_64_CHARACTERS

# Staging JWT Secret
# Used for staging environment (staging.your-domain.workers.dev)
# Should be different from production JWT_SECRET
STAGING_JWT_SECRET=CHANGE_THIS_TO_A_DIFFERENT_STRONG_RANDOM_SECRET_FOR_STAGING

# Development: Default Password for Seed Data
# Used for development and staging environments only (NOT production)
# ⚠️ CRITICAL: Only applies to non-production environments
# ⚠️ CRITICAL: Production will reject any default password setting
# ⚠️ CRITICAL: Users must reset passwords through password reset flow in production
# If not set, a secure random password will be generated automatically
# Default: 24-character random string with special characters (A-Z, a-z, 0-9, !@#$%^&*)
# Example: X7kP$m2@nQ9vL#5wR8!eZ1&h
DEFAULT_PASSWORD=change_this_password_for_development_only

# Cloudflare Workers Configuration
# These are typically set by Wrangler automatically
# CF_ACCOUNT_ID=your-account-id
# CF_API_TOKEN=your-api-token

# Security: Rate Limiting (optional overrides)
# Override default rate limiting configuration if needed
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_AUTH_WINDOW_MS=900000
# RATE_LIMIT_AUTH_MAX_REQUESTS=5

# Application Logging
# Log level for application logging (default: debug in dev, info in production)
# Options: debug, info, warn, error
VITE_LOG_LEVEL=info

# School Configuration
# School name displayed throughout the application
# Default: SMA Negeri 1 Jakarta
SCHOOL_NAME=SMA Negeri 1 Jakarta

# Academic Year
# Current academic year in format YYYY-YYYY
# Default: 2024-2025
ACADEMIC_YEAR=2024-2025

# Semester
# Current semester (1 or 2)
# Default: 1
SEMESTER=1

# Registration Control
# Allow new user registration (true or false)
# Default: true
ALLOW_REGISTRATION=true

# Maintenance Mode
# Enable maintenance mode (restricts access to admin only)
# Default: false
MAINTENANCE_MODE=false
